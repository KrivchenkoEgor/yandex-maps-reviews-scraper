# LESSONS.md — Журнал уроков

Этот файл агент заполняет автоматически после успешных задач.

## Правила
- Записывать только **новые** наблюдения: селекторы на Яндексе, приёмы обхода антибота, грабли
- Рутина (открыл страницу, прочитал файл) не логируется
- Уроки, которые повторяются или доказали ценность, предлагаются к закреплению в `AGENTS.md` — добавляются только с одобрения пользователя

## Формат урока

### [YYYY-MM-DD HH:MM] Название задачи
**Тип задачи:** (парсинг / экспорт / мониторинг / отладка)
**Что сделано:** кратко
**Найденный селектор:** (CSS/XPath для извлечения данных)
**Рабочий приём:** (приём кода, подход)
**Грабли:** (на что наткнулся и как обойти)
**Оценка пользователя:** ✅ / без оценки

---

## Уроки

### [2026-08-24 18:20] Магнит 222 + Добрянка 600/1750, фото get-altay vs yapic, Ещё, limited, API
**Тип задачи:** парсинг Яндекс.Карт (SPA) + антибот
**Что сделано:** 222 Магнит (CTwsUYyk) 5 страниц API, Добрянка 600/1750 (35 страниц, 12 лимит by_relevance_org), полный текст Олега 324 `бумерангом... 😁` без `А…Ещё`, фото `get-altay` 2/1, Сергей 2019-12-01 + ответ 2025-06-01, имя Добрянка из h1
**Найденный селектор:** `scroll__container` (13042/656) для триггера `fetchReviews?page=1..35&pageSize=50`, `div.business-review-view` (725 карточек → 350 после фильтра), `div.business-review-view__photos img[src*="get-altay"]` (review), `get-yapic/islands-68` — аватарка
**Рабочий приём:** API `fetchReviews?businessId=...&page=&ranking=by_relevance_org` через `page.on('response')` + человечный `mouse.wheel 300-700×3-5` + `human_mouse_move` 20-50 шагов + `hover`, `disable-blink-features=AutomationControlled`, `webdriver=undefined`, `has_touch` рандом; `reviewId` из API (уникален) + фолбэк `hash(full_text|author|date|rating|likes|photos_len)`; `params.count 222/1750 totalPages` из API
**Грабли:** `is_captcha_page` по raw HTML ловил `captchapgrd` скрипт → ложная капча → `BeautifulSoup` без `script/style` + `innerText` + `is_visible`; `hash(text[:120]+author)` коллапсил `Хороший выбор` → `hash(full...)`; `shop_info` давал `350` (визиты) вместо `222` → `params.count` из API + `h1` без класса; `photo` `yapic` vs `altay`; `limited` 158 байт после 3 прогонов → 60с кулдаун + `desktop UA` (iPhone блочит); `8000` занят `python3.1:irdmi` → `8001` + `start.sh/stop.sh`
**Оценка пользователя:** ✅ (Олег 324, Валерия 898, Сергей найден)

### Типовые темы для записи
- Новые селекторы для отзывов (Яндекс часто меняет вёрстку)
- Паттерны капчи и способы их обнаружения
- Особенности парсинга дат вида «3 дня назад», «вчера»
- Сработавшие User-Agent (какие не блокируются)
- Оптимальные значения пауз для разных регионов
- Приёмы извлечения oid из разных форматов URL
- Особенности работы с фото-лентой отзывов
- Приёмы работы с бесконечным скроллом

### [2026-08-29 03:40] Apple-дизайн Gradio 4.44: кастомный CSS в gr.Blocks
**Тип задачи:** UI/UX редизайн (app/ui.py, только build_ui + CSS, логика не тронута)
**Что сделано:** тема Default(blue/slate) + системный шрифт `-apple-system` (SF Pro на маке), фон #f5f5f7, карточка .tabs 20px + тень, табы-сегмент-контрол (.tab-nav pill, .selected белый), кнопки-пилюли #0071e3 (background-image:none! — Soft/Default рисуют градиент), инпуты 12px с focus-ring, footer{display:none} скрывает брендинг Gradio, dataframes с headers= вместо пустых «1 2 3»
**Грабли:** фон полей красит `.form` (не `.block`) → `.tabs .form{background:transparent}`; правило `.tabs .wrap` ломает кнопку загрузки файла (внутри неё служебный div.wrap → узкая рамка) — не стилизовать .wrap глобально; оттенки темы — только строками `primary_hue="blue"` (gr.themes.slate не существует в 4.44)
**Проверка:** живой регресс после редизайна — скрейп Быстронома 600/835 (100% совпадение review_id и метрик с предыдущим днём), экспорты xlsx/csv/json, мониторинг add+list, пакетный режим с файлом, ошибка станции CTHZjT09 (businessId ≠ oid)

### [2026-08-29 04:00] Дубли прогресс-баров в Gradio 4.44: show_progress и скрытие выходов
**Грабли:** прогресс Gradio рисуется на КАЖДОМ видимом выходном компоненте события; "minimal" не помогает (всё равно индикатор на каждом выходе, просто компактнее)
**Рабочий приём:** `show_progress="minimal"` + прятать лишние выходы до готовности: таблица/файлы создаются `visible=False`, обработчик возвращает `gr.update(value=..., visible=True)`; во время работы виден один выход → один прогресс-бар, пустые остовы не мозолят глаза

### [2026-08-29 05:50] Многопользовательский режим: изоляция через файл БД на пользователя
**Рабочий приём:** Database(path) уже принимал произвольный путь → личная БД db/users/uid_<id>/ya_ot.db дала изоляцию БЕЗ изменения схемы shops/reviews/monitoring; реестр аккаунтов отдельно в db/users/users.db
**Грабли:** при перезапуске сервера старый процесс может держать порт (address already in use, новый процесс молча умирает после lifespan — health отвечает СТАРЫЙ процесс) → перед стартом kill всех python -m app.main и проверять, что в / появился новый элемент UI; Gradio refs компонента ≠ порядок полей с одинаковым label («Email» ×4 — заполнять по снапшоту блока, иначе fill уходит в чужое поле)
**Безопасность:** PBKDF2 200k итераций, secrets.compare_digest, 6-значный код 24ч, блокировка 5 входов/60с; код хранится открыто — норм для локального инструмента, для публичного деплоя хэшировать и включать SMTP

### [2026-08-29 12:30] Пагинация fetchReviews: телепорт-скролл не триггерит page=2 (50/835)
**Грабли:** `scrollTop = scrollHeight` (телепорт в низ) не создаёт событий скролла, если список после загрузки уже приклеен ко дну → SPA не запрашивает page=2, сбор стопится на 50; в ~30% сессий 29.08 выгрузки получали 50 вместо 600. Прямые запросы fetchReviews с подменой page работают нестабильно, с подменой ranking — сразу 400 (подпись `s` в query покрывает ranking)
**Рабочий приём (исправлено после A/B 13:46):** основная прокрутка — прежняя связка «колесо 2-4х в случайных точках + human_mouse_move + ТЕЛЕПОРТ scrollTop=scrollHeight + ховер карточки» — она и даёт стабильные 600 (A/B: старый код 600, моя «плавная» прокрутка без телепорта — только 100-250). Телепорт ко дну — рабочий триггер пагинации в норме; «приклеенный ко дну» список — редкий случай (~1/3 сессий 29.08), против него спасатели при простое: прямая догрузка page=2.. (иногда проходит), перезагрузка страницы до 2 раз, stall_limit 10 при total>300 и собрано<150. Клик «По новизне» ломается рекламным баннером (div#Y-A-* перехватывает pointer events) — только JS el.click()
**Ошибка вывода:** версии про «серверный троттлинг после 03:48» не было — короткие сборы были (а) редкие зависшие сессии старого кода, (б) моя собственная прокрутка-замена; A/B на одной ссылке в 13:46: старый код 600, мой 150, откат 600
